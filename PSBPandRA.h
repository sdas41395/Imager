//header for it
#include <userint.h>
#ifdef __cplusplus
    extern "C" {
#endif
		
	// extern void PS_BP_Off(void);
	// extern void PS_RA_On(void);
	// extern void PS_RA_Off(void);
	// extern void PS_BP_On(void);
	// extern int ToggleBlueActEnHighFn (int bBA_En_OnOff);
	// extern int ToggleDigOutFn (int OnOff, char * chan );
	// extern int CVICALLBACK ToggleDigOut (int panel, int control, int event,
	// 					  void *callbackData, int eventData1, int eventData2);
	// extern int CVICALLBACK ToggleDO (int panel, int control, int event,
	// 					  void *callbackData, int eventData1, int eventData2);
	// extern int CVICALLBACK AO_BAVollt (int panel, int control, int event,
	// 						void *callbackData, int eventData1, int eventData2);
	// extern int ToggleCntrFn(int control,int control_value);
	
	// extern int AO_BAVoltFn(double voltBA);
	
	// extern int AO_BPV_I_Fn(double v_BP, double i_BP);
	

	
#ifdef __cplusplus
    }
#endif
